# GitHub Action workflow file

name: FastLane Try Build

on:
  workflow_call:
  workflow_dispatch:

jobs:
  build-for-pr:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
    - run: echo "::notice::GitHub Action fastlane scan"
    - run: fastlane scan
