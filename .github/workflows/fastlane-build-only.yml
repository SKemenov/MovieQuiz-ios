# GitHub Action workflow file

name: FastLane scan

on:
  workflow_call:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
    - name: Build only
      run: fastlane scan

